'use strict';

angular.module('MLDS')
    .controller('UserRegistrationFlowController',
        [ '$scope', '$log', function ($scope, $log) {
        	
        	$scope.user = {};
        	
        }
    ]);