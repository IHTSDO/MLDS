'use strict';

angular.module('MLDS')
    .controller('DashboardController',
        [ '$scope', '$log', function ($scope, $log) {
        	
        	console.log('Dashboard Controller');
        }
    ]);