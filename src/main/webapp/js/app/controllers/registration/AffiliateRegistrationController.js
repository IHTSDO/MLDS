'use strict';

angular.module('MLDS')
    .controller('AffiliateRegistrationController',
        [ '$scope', '$log',  function ($scope, $log) {
        	
        	$scope.affilateform = {};
        	
        	$scope.collapsePanel = {};
        	
        }
    ]);