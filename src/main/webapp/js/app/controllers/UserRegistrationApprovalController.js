'use strict';

angular.module('MLDS')
    .controller('UserRegistrationApprovalsController',
        [ '$scope', '$log', function ($scope, $log) {
        	
        	$scope.status = "processing";
        }
    ]);