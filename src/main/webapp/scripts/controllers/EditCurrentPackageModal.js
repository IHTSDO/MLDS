'use strict';

angular.module('MLDS').controller('EditCurrentPackageModal', ['$scope', '$log', '$modalInstance',
    function ($scope, $log, $modalInstance) {
		$log.log('EditCurrentPackageModal', $scope.test);
		
    }]);